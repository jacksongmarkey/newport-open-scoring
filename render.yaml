services:
  - type: web
    name: newport-open-scoring
    runtime: node
    plan: free
    buildCommand: npm install
    startCommand: npm start
    healthCheckPath: /healthz
    autoDeploy: true
    envVars:
      - key: NODE_VERSION
        value: 20.11.1
      - key: ADMIN_TOKEN
        generateValue: true
